* always remember to reduce size ,or size++
* allocate memory, set newnode.next=null,set value of newnode,
remove:
temp.next=temp.next.next 



rever using swap: we can swap using getdata()
        lf=getData(left)
        rtgetData(right);
        swap(lf,rt)
  

